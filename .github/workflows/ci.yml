name: CI 

on:
  push:
  workflow_dispatch:

jobs:
  rss_validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e
        with:
          sparse-checkout: rss.xml
      - name: RSS validation
        run: |
          npx feed-validator rss.xml
